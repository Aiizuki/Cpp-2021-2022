/*template <typename T>
WrapPointeur<T>::WrapPointeur(T* _valeur){
  this->valeur = _valeur;
}

template <typename T>
WrapPointeur<T>::~WrapPointeur(){
  delete *(this->valeur);
  delete this->valeur;
}

template <typename T>
struct Noeud<T>* WrapPointeur<T>::Unwrap(){
  return *(this->valeur);
}*/
